# CenterNet 声呐 STFT 目标识别模型结构改进总结

本文档基于当前项目代码进行整理，重点总结模型结构从原始 ResNet50-CenterNet、SwinV2Tiny-CenterNet 到 SwinV2Tiny-FPN-CenterNet、CNNStem-SwinV2Tiny-FPN-CenterNet 的结构演进。本文只描述代码中已经实现的结构，不包含训练结果和性能提升结论。

---

## 一、原始模型结构

### 1. 输入 STFT 时频矩阵

当前声呐数据集定义在：

```text
src/lib/datasets/dataset/sonar.py
```

核心类为：

```python
class SonarDataset(Dataset)
```

代码中数据集默认输入分辨率为：

```python
default_resolution = [256, 256]
```

每个样本读取 `.mat` 文件中的：

```python
tf_matrix
```

然后将单通道 STFT 矩阵复制为 3 通道输入：

```python
inp = np.stack([tf, tf, tf], axis=0)
```

因此单样本输入 shape 为：

```text
[3, 256, 256]
```

训练 batch 输入 shape 为：

```text
[B, 3, 256, 256]
```

其中 `B` 为 batch size。

---

### 2. CenterNet Head 输出

当前 `ctdet` 任务的 head 配置来自：

```text
src/lib/opts.py
```

在 `update_dataset_info_and_set_heads()` 中，`ctdet` 任务默认构造：

```python
opt.heads = {
    'hm': opt.num_classes,
    'wh': 2
}
```

如果 `reg_offset=True`，继续加入：

```python
'reg': 2
```

当前 `SonarDataset.num_classes = 1`，因此当前 CenterNet Head 输出为：

```text
hm  : [B, 1, Hout, Wout]
wh  : [B, 2, Hout, Wout]
reg : [B, 2, Hout, Wout]
```

由于默认输入为 `256×256`，`down_ratio=4`，所以输出 heatmap 尺寸为：

```text
Hout = Wout = 256 / 4 = 64
```

最终输出 shape 为：

```text
hm  : [B, 1, 64, 64]
wh  : [B, 2, 64, 64]
reg : [B, 2, 64, 64]
```

三个 head 的含义为：

| Head | 输出通道 | 作用 |
|---|---:|---|
| `hm` | 1 | 目标响应热力图，reverb 作为背景处理 |
| `wh` | 2 | 目标宽高或等效尺度回归 |
| `reg` | 2 | heatmap 网格内的中心点偏移回归 |

当前模型均保持 CenterNet 原始输出字典格式：

```python
{
    'hm': ...,
    'wh': ...,
    'reg': ...
}
```

并且 forward 返回：

```python
[ret]
```

以兼容现有 CenterNet trainer 和 loss。

---

### 3. ResNet50-CenterNet 结构

ResNet50-CenterNet 定义在：

```text
src/lib/models/networks/msra_resnet.py
```

核心类为：

```python
class PoseResNet(nn.Module)
```

模型构建入口为：

```python
def get_pose_net(num_layers, heads, head_conv)
```

`res_50` 通过 `src/lib/models/model.py` 中的 `create_model()` 创建，最终调用：

```python
get_pose_net(num_layers=50, heads=heads, head_conv=head_conv)
```

ResNet50 配置来自：

```python
resnet_spec = {
    50: (Bottleneck, [3, 4, 6, 3])
}
```

结构流程为：

```text
STFT input [B, 3, 256, 256]
→ ResNet50 backbone
→ 3 层 deconv 上采样
→ CenterNet Head
→ hm / wh / reg
```

其中 deconv 部分为 3 层转置卷积：

```python
self.deconv_layers = self._make_deconv_layer(
    3,
    [256, 256, 256],
    [4, 4, 4],
)
```

ResNet50 backbone 提取深层特征后，deconv 将特征恢复到 `1/4` 输入分辨率，即 `64×64`，然后进入 CenterNet Head。

最终输出：

```text
hm  : [B, 1, 64, 64]
wh  : [B, 2, 64, 64]
reg : [B, 2, 64, 64]
```

---

### 4. SwinV2Tiny-CenterNet 结构

SwinV2Tiny-CenterNet 定义在：

```text
src/lib/models/networks/swin.py
```

核心类为：

```python
class SwinCenterNet(nn.Module)
```

在 `src/lib/models/model.py` 中注册的 arch 名称为：

```text
swin_tiny
```

当前 Swin backbone 使用 timm 创建：

```python
self.backbone = timm.create_model(
    model_name,
    pretrained=pretrained,
    features_only=True,
    out_indices=(3,)
)
```

其中 `model_name` 为：

```text
swinv2_tiny_window8_256
```

这表示当前 `swin_tiny` 只取 SwinV2 Tiny 最后一层特征，即 C5。

对于输入：

```text
[B, 3, 256, 256]
```

Swin 最后一层特征在 timm 中输出为 NHWC 格式：

```text
[B, 8, 8, 768]
```

代码中转换为 NCHW：

```python
feat = feat.permute(0, 3, 1, 2).contiguous()
```

转换后 shape 为：

```text
[B, 768, 8, 8]
```

随后经过 3 层转置卷积上采样：

```python
768 → 256 → 128 → 64
```

空间尺寸变化为：

```text
8×8 → 16×16 → 32×32 → 64×64
```

最终进入 CenterNet Head，输出：

```text
hm  : [B, 1, 64, 64]
wh  : [B, 2, 64, 64]
reg : [B, 2, 64, 64]
```

---

## 二、SwinV2Tiny-CenterNet 的原始问题

当前 `swin_tiny` 的关键限制在于：

```python
out_indices=(3,)
```

也就是只使用 SwinV2 Tiny 的最后一层深层特征 C5。

### 1. 只使用 C5 深层特征

对于 `256×256` 输入，C5 的空间分辨率约为：

```text
[B, 768, 8, 8]
```

这一级特征具有较强的语义表达能力，但空间分辨率很低。对于自然图像分类任务，这种深层语义特征通常足够；但对于声呐 STFT 时频矩阵中的亮点检测、小目标定位和局部结构识别，仅依赖 C5 可能存在不足。

### 2. 对 STFT 亮点和小目标检测的影响

主动声呐 STFT 时频矩阵中，目标回波可能表现为：

- 局部能量集中的亮点；
- 窄带或斜率结构；
- 头部、尾部、中心点等局部关键区域；
- 与混响亮点形态相近的弱目标响应。

这些结构往往依赖较高分辨率的局部纹理信息。C5 特征经过多次 patch merging 后，空间尺寸已经压缩到 `8×8`，局部细节和小尺度峰值可能被弱化。

### 3. 单尺度深层特征的潜在不足

当前 `swin_tiny` 的信息流为：

```text
STFT input
→ SwinV2 Tiny C5
→ Deconv 上采样
→ CenterNet Head
```

其潜在问题是：

1. 浅层高分辨率信息没有直接参与检测；
2. 小目标或局部亮点依赖的细粒度纹理可能在深层压缩中丢失；
3. 只靠 deconv 从 C5 恢复 `64×64` heatmap，可能难以还原精确位置；
4. 混响亮点与真实目标亮点的细节差异可能没有被充分建模；
5. 对多尺度目标形态、头尾结构、局部斜率纹理的表达能力受限。

因此，后续结构改进的核心方向是：在保持 CenterNet Head 不变的前提下，引入 Swin 多层输出和多尺度融合结构。

---

## 三、新增 SwinV2Tiny-FPN-CenterNet

### 1. 新增文件、类和 arch

新增模型文件：

```text
src/lib/models/networks/swin_fpn.py
```

新增核心类：

```python
class SwinV2TinyFPNCenterNet(nn.Module)
```

在模型注册文件中：

```text
src/lib/models/model.py
```

新增 arch 名称：

```text
swin_tiny_fpn
```

创建逻辑为：

```python
if arch == 'swin_tiny_fpn':
    return SwinV2TinyFPNCenterNet(
        heads=heads,
        head_conv=head_conv,
        model_name='swinv2_tiny_window8_256',
        pretrained=True,
        fpn_channels=128,
        use_cnn_stem=False,
        in_chans=3
    )
```

该结构在不破坏原 `swin_tiny` 的基础上，新增了一个 FPN 版本。

---

### 2. Swin backbone 多尺度输出

原 `swin_tiny` 使用：

```python
out_indices=(3,)
```

新增 `swin_tiny_fpn` 改为：

```python
out_indices=(0, 1, 2, 3)
```

因此 backbone 同时输出四个 stage 的特征：

```text
C2, C3, C4, C5
```

对于输入：

```text
[B, 3, 256, 256]
```

SwinV2 Tiny 多尺度特征预期 shape 为：

| 特征层 | timm 输出格式 | 转换后格式 |
|---|---|---|
| C2 | `[B, 64, 64, 96]` | `[B, 96, 64, 64]` |
| C3 | `[B, 32, 32, 192]` | `[B, 192, 32, 32]` |
| C4 | `[B, 16, 16, 384]` | `[B, 384, 16, 16]` |
| C5 | `[B, 8, 8, 768]` | `[B, 768, 8, 8]` |

---

### 3. timm 输出格式转换

timm 的 Swin 特征通常为 NHWC 格式，即：

```text
[B, H, W, C]
```

而 PyTorch 卷积模块期望 NCHW：

```text
[B, C, H, W]
```

因此 `SwinV2TinyFPNCenterNet` 中定义了 `_to_nchw()`：

```python
@staticmethod
def _to_nchw(feat):
    if feat.ndim != 4:
        raise ValueError('Expected a 4D feature map.')
    if feat.shape[1] < feat.shape[-1]:
        return feat.permute(0, 3, 1, 2).contiguous()
    return feat.contiguous()
```

其作用是将 Swin 输出统一转换为 `[B, C, H, W]`，便于后续 FPN 使用 `Conv2d` 进行通道对齐和融合。

---

### 4. SimpleFPN 结构

新增 FPN 类为：

```python
class SimpleFPN(nn.Module)
```

FPN 输入为：

```text
C2, C3, C4, C5
```

每层先通过 `1×1 Conv` 对齐通道数：

```python
self.laterals = nn.ModuleList([
    nn.Conv2d(ch, fpn_channels, kernel_size=1)
    for ch in in_channels
])
```

当前 `fpn_channels=128`。

因此通道统一为：

```text
C2/C3/C4/C5 → 128 channels
```

随后执行自顶向下融合：

```python
p5 = lateral(C5)
p4 = lateral(C4) + upsample(P5)
p3 = lateral(C3) + upsample(P4)
p2 = lateral(C2) + upsample(P3)
```

对应结构为：

```text
C5 → P5
C4 + upsample(P5) → P4
C3 + upsample(P4) → P3
C2 + upsample(P3) → P2
```

每个 P 层融合后接 `3×3 smoothing conv`：

```python
self.smooth = nn.ModuleList([
    ConvBnRelu(fpn_channels, fpn_channels, kernel_size=3, padding=1)
    for _ in in_channels
])
```

其中 `ConvBnRelu` 结构为：

```text
Conv2d → BatchNorm2d → ReLU
```

最后将 P3、P4、P5 全部上采样到 P2 尺寸，并与 P2 相加：

```python
fused = (
    p2
    + upsample(p3, p2)
    + upsample(p4, p2)
    + upsample(p5, p2)
)
```

完整 FPN 路径为：

```text
C2/C3/C4/C5
→ 1×1 lateral conv
→ top-down 融合
→ 3×3 smoothing conv
→ P2/P3/P4/P5
→ P3/P4/P5 上采样到 P2
→ fused feature
→ CenterNet Head
```

---

### 5. FPN 输出 shape

对于输入：

```text
[B, 3, 256, 256]
```

FPN 输出特征 shape 为：

| FPN 特征 | Shape |
|---|---|
| P2 | `[B, 128, 64, 64]` |
| P3 | `[B, 128, 32, 32]` |
| P4 | `[B, 128, 16, 16]` |
| P5 | `[B, 128, 8, 8]` |
| fused | `[B, 128, 64, 64]` |

最终 `fused` 特征输入 CenterNet Head：

```python
ret[head] = self.__getattr__(head)(fused)
```

最终输出 shape 为：

```text
hm  : [B, 1, 64, 64]
wh  : [B, 2, 64, 64]
reg : [B, 2, 64, 64]
```

---

### 6. 相比原 `swin_tiny` 的改进意义

`swin_tiny_fpn` 相比原 `swin_tiny` 的核心改进在于：由单一深层特征 C5 改为多尺度特征融合。

原 `swin_tiny`：

```text
只使用 C5: [B, 768, 8, 8]
```

新增 `swin_tiny_fpn`：

```text
同时使用 C2/C3/C4/C5
```

其结构意义包括：

1. **引入多尺度特征**  
   C2、C3、C4、C5 分别包含不同空间尺度的信息，使模型不再只依赖单一 `8×8` 深层特征。

2. **保留浅层高分辨率信息**  
   C2 的分辨率为 `64×64`，与最终 CenterNet heatmap 尺寸一致，能够保留更多局部位置信息。

3. **增强 STFT 亮点、小目标和局部纹理表达**  
   STFT 中的局部亮点、窄带结构、头尾结构和局部斜率信息更依赖高分辨率浅层特征。FPN 将浅层细节与深层语义融合，有助于提高目标区域表达能力。

4. **减少单纯从 C5 上采样恢复细节的压力**  
   原 `swin_tiny` 需要从 `8×8` C5 通过上采样恢复到 `64×64`。FPN 直接融合 `64×64` 的 C2，可降低细节恢复难度。

5. **为后续 BiFPN / Quality Head 做接口准备**  
   当前 `SimpleFPN` 已经形成明确的多尺度 neck 接口，后续可以在此基础上替换为 BiFPN，或在 fused feature 后增加质量预测分支。

---

## 四、新增 CNNStem-SwinV2Tiny-FPN-CenterNet

### 1. 新增参数和 arch

在 `SwinV2TinyFPNCenterNet` 中新增参数：

```python
use_cnn_stem=False
in_chans=3
```

默认值为：

```text
use_cnn_stem=False
```

因此默认情况下，不启用 CNN Stem，保持 `swin_tiny_fpn` 原始数据流。

在 `src/lib/models/model.py` 中新增 arch：

```text
swin_tiny_fpn_cnnstem
```

创建逻辑为：

```python
if arch == 'swin_tiny_fpn_cnnstem':
    return SwinV2TinyFPNCenterNet(
        heads=heads,
        head_conv=head_conv,
        model_name='swinv2_tiny_window8_256',
        pretrained=True,
        fpn_channels=128,
        use_cnn_stem=True,
        in_chans=3
    )
```

---

### 2. CNN Stem 结构

当 `use_cnn_stem=True` 时，模型在进入 Swin backbone 前增加 CNN Stem：

```python
self.cnn_stem = nn.Sequential(
    nn.Conv2d(in_chans, 32, kernel_size=3, stride=1, padding=1),
    nn.BatchNorm2d(32),
    nn.SiLU(inplace=True),
    nn.Conv2d(32, 3, kernel_size=3, stride=1, padding=1),
    nn.BatchNorm2d(3),
    nn.SiLU(inplace=True),
)
```

结构可写为：

```text
Conv2d(in_chans, 32, 3, 1, 1)
→ BatchNorm2d(32)
→ SiLU
→ Conv2d(32, 3, 3, 1, 1)
→ BatchNorm2d(3)
→ SiLU
```

其中第二个卷积输出为 3 通道，因此后续仍然可以输入原始 SwinV2 Tiny backbone。

---

### 3. `use_cnn_stem=True` 与 `False` 的数据流区别

当 `use_cnn_stem=False` 时：

```text
STFT input [B, 3, 256, 256]
→ SwinV2 Tiny
→ C2/C3/C4/C5
→ FPN
→ CenterNet Head
→ hm/wh/reg
```

当 `use_cnn_stem=True` 时：

```text
STFT input [B, in_chans, 256, 256]
→ CNN Stem
→ [B, 3, 256, 256]
→ SwinV2 Tiny
→ C2/C3/C4/C5
→ FPN
→ CenterNet Head
→ hm/wh/reg
```

当前 dataset 默认仍然使用 3 通道输入：

```text
[B, 3, 256, 256]
```

因此当前 `swin_tiny_fpn_cnnstem` 在 `create_model()` 中使用：

```python
in_chans=3
```

不过 `SwinV2TinyFPNCenterNet` 类本身的 CNN Stem 第一层支持：

```text
in_chans=1 或 in_chans=3
```

这意味着后续如果数据集改为单通道 STFT 输入，也可以通过 `in_chans=1` 使用 CNN Stem 将其映射为 3 通道后再送入 Swin。

---

### 4. CNN Stem 的结构意义

CNN Stem 的加入主要针对 STFT 时频矩阵与自然图像之间的差异。

Swin Transformer 原始 patch embedding 更偏向自然图像输入，而声呐 STFT 矩阵中的有效信息往往体现为：

- 局部能量集中区域；
- 窄带亮线；
- 时频斜率纹理；
- 目标头尾结构；
- 混响亮点与真实目标亮点之间的局部形态差异。

CNN Stem 在进入 Swin 前先使用局部卷积建模邻域纹理，因此具有以下作用：

1. **在进入 Swin 前提取 STFT 局部时频纹理**  
   `3×3 Conv` 对局部邻域敏感，适合捕获局部亮点、窄带结构和纹理边缘。

2. **增强局部能量集中区域表达**  
   对于目标亮点、小尺度响应、头尾局部峰值，CNN Stem 可以在 patch 化之前先进行局部增强。

3. **缓解直接将 STFT 当作自然图像输入 Swin 的问题**  
   STFT 时频矩阵不是自然图像，直接使用 ImageNet 预训练视觉 backbone 可能存在输入分布差异。CNN Stem 可以作为浅层适配模块。

4. **可能提升混响亮点和真实目标亮点的区分能力**  
   混响亮点与真实目标亮点可能在整体强度上相似，但在局部形态、连续性、斜率和头尾结构上存在差异。CNN Stem 为这些局部差异提供了更早的建模入口。

需要注意：当前代码中 CNN Stem 只是结构上加入，尚未给出完整实验结果，因此不能直接声称性能已经提升。

---

## 五、模型结构对比表

| 实验编号 | arch 名称 | 模型结构 | Backbone | 是否使用 FPN | 是否使用 CNN Stem | 输出 head | 实验目的 |
|---|---|---|---|---|---|---|---|
| E0 | `res_50` | ResNet50 + Deconv + CenterNet Head | ResNet50 | 否 | 否 | `hm`, `wh`, `reg` | 作为 CNN backbone 的 CenterNet baseline，验证传统卷积特征在 STFT 目标检测中的效果 |
| E1 | `swin_tiny` | SwinV2 Tiny C5 + Upsample + CenterNet Head | SwinV2 Tiny | 否 | 否 | `hm`, `wh`, `reg` | 引入 Transformer backbone，比较 Swin 深层语义特征与 ResNet baseline 的差异 |
| E2 | `swin_tiny_fpn` | SwinV2 Tiny C2/C3/C4/C5 + SimpleFPN + CenterNet Head | SwinV2 Tiny | 是 | 否 | `hm`, `wh`, `reg` | 从单尺度 C5 改为多尺度融合，增强小目标、亮点和局部纹理表达 |
| E3 | `swin_tiny_fpn_cnnstem` | CNN Stem + SwinV2 Tiny C2/C3/C4/C5 + SimpleFPN + CenterNet Head | CNN Stem + SwinV2 Tiny | 是 | 是 | `hm`, `wh`, `reg` | 在 Swin 前加入局部卷积适配模块，增强 STFT 局部时频纹理建模能力 |

---

## 六、结构流程图

### 1. ResNet50-CenterNet

```text
STFT input [B, 3, 256, 256]
→ ResNet50
→ Deconv [B, 256, 64, 64]
→ CenterNet Head
→ hm  [B, 1, 64, 64]
→ wh  [B, 2, 64, 64]
→ reg [B, 2, 64, 64]
```

---

### 2. SwinV2Tiny-CenterNet

```text
STFT input [B, 3, 256, 256]
→ SwinV2 Tiny C5 [B, 768, 8, 8]
→ Upsample [B, 64, 64, 64]
→ CenterNet Head
→ hm  [B, 1, 64, 64]
→ wh  [B, 2, 64, 64]
→ reg [B, 2, 64, 64]
```

---

### 3. SwinV2Tiny-FPN-CenterNet

```text
STFT input [B, 3, 256, 256]
→ SwinV2 Tiny backbone
   ├─ C2 [B, 96, 64, 64]
   ├─ C3 [B, 192, 32, 32]
   ├─ C4 [B, 384, 16, 16]
   └─ C5 [B, 768, 8, 8]
→ SimpleFPN
   ├─ P2 [B, 128, 64, 64]
   ├─ P3 [B, 128, 32, 32]
   ├─ P4 [B, 128, 16, 16]
   └─ P5 [B, 128, 8, 8]
→ fused P2 [B, 128, 64, 64]
→ CenterNet Head
→ hm  [B, 1, 64, 64]
→ wh  [B, 2, 64, 64]
→ reg [B, 2, 64, 64]
```

---

### 4. CNNStem-SwinV2Tiny-FPN-CenterNet

```text
STFT input [B, 3, 256, 256]
→ CNN Stem [B, 3, 256, 256]
→ SwinV2 Tiny backbone
   ├─ C2 [B, 96, 64, 64]
   ├─ C3 [B, 192, 32, 32]
   ├─ C4 [B, 384, 16, 16]
   └─ C5 [B, 768, 8, 8]
→ SimpleFPN
→ fused P2 [B, 128, 64, 64]
→ CenterNet Head
→ hm  [B, 1, 64, 64]
→ wh  [B, 2, 64, 64]
→ reg [B, 2, 64, 64]
```

---

## 七、当前模型改进的核心思想

1. **从单尺度深层特征改为多尺度特征融合**  
   原 `swin_tiny` 只使用 C5。新增 `swin_tiny_fpn` 同时使用 C2、C3、C4、C5，使模型能够结合浅层细节和深层语义。

2. **引入 FPN 保留高分辨率浅层信息**  
   C2 的空间分辨率为 `64×64`，与 CenterNet 输出 heatmap 一致。FPN 将 C2 的高分辨率定位信息与 C5 的深层语义信息融合，更适合 STFT 亮点和小目标检测。

3. **引入 CNN Stem 强化 STFT 局部纹理建模**  
   CNN Stem 在 Swin patch embedding 前使用 `3×3` 卷积提取局部时频纹理，有助于建模局部亮点、窄带结构、斜率纹理和头尾结构。

4. **保持 CenterNet Head 不变，保证训练和评估流程兼容**  
   所有模型仍输出 `hm/wh/reg`，并返回 `[ret]`，因此可以继续使用当前 `CtdetLoss`、dataset label 格式和 decode/eval 流程。

5. **为后续结构扩展预留接口**  
   当前 `SimpleFPN` 和 `fused feature` 已经形成清晰的 neck 输出接口，可作为后续 BiFPN、Quality Head、动静目标分类分支或辅助任务分支的基础。

---

## 八、当前尚未加入的模块

根据当前代码，以下模块尚未加入：

| 模块 | 当前状态 |
|---|---|
| BiFPN | 尚未加入；当前实现为普通 top-down SimpleFPN |
| Quality Head | 尚未加入；当前 head 仍为 `hm/wh/reg` |
| 动静目标分类分支 | 尚未加入 |
| Doppler / 时频斜率辅助分支 | 尚未加入 |
| 对比学习损失 | 尚未加入 |
| dataset 修改 | 未修改；当前仍使用 `SonarDataset` 的原输入与标签格式 |
| loss 修改 | 未修改；当前仍使用 `CtdetLoss` 中的 `hm_loss/wh_loss/off_loss` |
| decode 修改 | 未修改；当前仍使用现有 CenterNet decode 或 `eval_new.py` 中的 decode |
| eval 修改 | 当前结构改进未引入新的评估逻辑 |

当前新增结构只集中在模型文件和模型注册层面，未改变训练标签、loss、decode 或评估指标定义。

---

## 九、可用于论文或开题/中期汇报的总结性表述

本阶段工作主要围绕主动声呐 STFT 时频矩阵目标识别任务中的 CenterNet 检测框架进行模型结构改进。在原有 ResNet50-CenterNet baseline 和 SwinV2Tiny-CenterNet 的基础上，针对 SwinV2Tiny 仅使用最后一层深层特征、浅层高分辨率局部信息利用不足的问题，设计并实现了 SwinV2Tiny-FPN-CenterNet 结构。该结构通过提取 SwinV2 Tiny 的 C2、C3、C4、C5 多尺度特征，并采用自顶向下的 FPN 融合方式，将浅层空间细节与深层语义信息进行结合，从而增强模型对 STFT 时频矩阵中局部亮点、小目标响应、窄带纹理和头尾结构的表达能力。

进一步地，为缓解直接将 STFT 时频矩阵作为自然图像输入 Transformer backbone 所带来的输入分布差异问题，本阶段在 SwinV2Tiny-FPN-CenterNet 前引入了可选 CNN Stem。CNN Stem 通过局部卷积在进入 Swin backbone 前提取时频邻域纹理特征，增强局部能量集中区域、斜率纹理和细粒度结构的建模能力。所有新增结构均保持 CenterNet Head 的 `hm`、`wh`、`reg` 输出形式不变，因此能够兼容现有数据集、损失函数、训练流程和评估流程。

总体而言，本阶段模型结构改进的目标是增强 CenterNet + SwinV2 Tiny 对主动声呐 STFT 时频矩阵的多尺度和局部纹理表达能力，提升模型对混响环境下真实目标亮点的建模能力，并为后续引入 BiFPN、Quality Head、动静目标分类分支和时频辅助任务分支提供结构基础。当前阶段尚未报告完整实验结果，因此不对性能提升作未经验证的结论。